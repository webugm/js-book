function swap(x) {
  var temp = x[0];
  x[0] = x[1];
  x[1] = temp;
}

var a = [1, 2];									// 將變數a的值設定為陣列 [1, 2]
alert('a[0] = ' + a[0] + ', a[1] = ' + a[1]);	// 顯示變數a的元素在交換之前的值
swap(a);										// 呼叫swap() 函式將參數的元素交換
alert('a[0] = ' + a[0] + ', a[1] = ' + a[1]);	// 顯示變數a的元素在交換之後的值
